import React from 'react';
import styled from '@emotion/styled';

export const OverviewContainer = styled.div<React.CSSProperties>`
  position: relative;
  flex-wrap: wrap;
  margin-top: 34px;
  & > 
`;

export const OverviewImageWrapper = styled.div<React.CSSProperties>`
  position: relative;
  width: 100%;
  height: 200px;
  margin-bottom: 16px;
`;

export const OverviewTitle = styled.div<React.CSSProperties>`
  border-left: 4px solid #0031b0;
  padding-left: 20px;
  color: #000;
  font-size: 32px;
  font-weight: 700;
  line-height: 36px;
  margin-bottom: 24px;
`;

